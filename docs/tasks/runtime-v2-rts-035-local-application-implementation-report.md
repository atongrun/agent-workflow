# RTS-035 Selected Local Workflow Application Closeout

## Result

`PASS` for the independently reversible disposable local application boundary.

The installed Python Runtime now composes the accepted immutable RunSpec, one checksummed atomic
Store/per-invocation journal, closed provider renderers, isolated workspace/trusted import and
Artifact policy behind one `LocalRuntimeApplication` with `run`, read-only `status` and exact local
`stop`. The complete implement/review/PASS, deterministic bounded rework/second-review/PASS and
BLOCKED paths pass without importing experiment code or production operations scripts.

Current RunLedger/checkpoint/outbox/inbox/RunEvidence remains the sole production authority and
recovery path. RTS-035 created only disposable Runtime v2 state and changed no production CLI,
default, retained state, Agent Bus, remote Git/GitHub, native lifecycle, migration or release path.

## Verification

- Independent TaskCard Gate Review at exact candidate `9d345321`: `PASS`, zero findings.
- Exact-head ordinary CI `32363592197`: Ruff, Linux and Windows suites, macOS runtime,
  resource/workflow/distribution validation and all installed-wheel jobs passed.
- Exact-head Binary Feasibility `32363592149`: every native and Rust comparison cell plus both
  aggregates passed.
- Final budgets: application 692/700 nonblank/noncomment lines; focused application tests and
  scripted provider 641/1,100; Core port/Store refinement below 180 net lines; no new dependency or
  persistent representation family.
- Scope audit found only frozen implementation paths before closeout, no generated/build/mass-
  formatting or unrelated files, and `git diff --check` passed.

## Closed failures

Candidate CI exposed two bounded defects before independent Review. Repair `8cd6fb1` made repeated
application `stop` return the already durable stopped snapshot instead of synthesizing a conflicting
sequence-bound command, and corrected the focused byte-preservation assertion. Repair `9d34532`
bound the disposable fixture to the existing platform `core.autocrlf` postflight semantics so
native Windows CRLF output passes the unchanged whitespace gate. Neither repair weakened the Store,
Artifact or exact-stop policy.

## Exact successor seam

Phase 3 is complete. The only successor authorized by this closeout is a separately frozen
**RTS-040 Phase 4A command/result envelope boundary**. It may define one versioned Stage-blind
Agent Bus command/result envelope and stable idempotency identity around the accepted local
application, with malformed or mismatched inputs denied before provider start and outgoing intent
kept distinct from external send/handler-success/ACK observations.

RTS-040 must remain independently reversible and begin with disposable no-model fixtures. It may
not change Agent Bus transport implementation/version ownership, touch production or retained
events, claim ACK from local state, dual-write legacy authority, migrate state, change defaults,
add a physical Coordinator or scheduler, start native-launcher work, release or delete
compatibility. A fresh isolated Mac-to-Windows no-model proof remains a later Phase 4A acceptance
gate after the local envelope boundary passes.

## Rollback and non-claims

Reverting the isolated application candidate removes only newly created Runtime v2 package/test
surfaces and disposable authority state. Production state requires no rollback because it was not
adopted or migrated. Transport, native lifecycle, distribution, dogfood, default, migration,
release and old-representation deletion remain behind later explicit gates.
