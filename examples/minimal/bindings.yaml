apiVersion: agent-workflow/v1alpha1
kind: BindingProfile
metadata:
  name: minimal
  version: 0.1.0
spec:
  bindings:
    planner:
      runner: shell
      mode: local
    implementer:
      runner: shell
      mode: local
